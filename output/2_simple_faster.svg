<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="230" onload="init(evt)" viewBox="0 0 1200 230" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
	}

	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
		}
		else if (e.target.id == "unzoom") unzoom();
		else if (e.target.id == "search") search_prompt();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
	}, false)

	// functions
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
		return;
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
	}

	// search
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)", "");
			if (term != null) {
				search(term)
			}
		} else {
			reset_search();
			searching = 0;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		var re = new RegExp(term);
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="230.0" fill="url(#background)"  />
<text id="title" x="600.00" y="24" >Flame Chart</text>
<text id="details" x="10.00" y="213" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="24" >Search</text>
<text id="matched" x="1090.00" y="213" > </text>
<g id="frames">
<g >
<title>large (1,516 samples, 9.89%)</title><rect x="1073.3" y="133" width="116.7" height="31.0" fill="rgb(218,10,13)" rx="2" ry="2" />
<text  x="1076.26" y="151.5" >large</text>
</g>
<g >
<title>codecs (572 samples, 3.73%)</title><rect x="108.4" y="101" width="44.1" height="31.0" fill="rgb(231,157,30)" rx="2" ry="2" />
<text  x="111.42" y="119.5" >codecs</text>
</g>
<g >
<title>_locale (109 samples, 0.71%)</title><rect x="393.9" y="101" width="8.4" height="31.0" fill="rgb(221,108,8)" rx="2" ry="2" />
<text  x="396.89" y="119.5" ></text>
</g>
<g >
<title>_bootlocale (398 samples, 2.60%)</title><rect x="371.6" y="133" width="30.7" height="31.0" fill="rgb(251,50,23)" rx="2" ry="2" />
<text  x="374.63" y="151.5" >_b..</text>
</g>
<g >
<title>encodings.utf_8 (225 samples, 1.47%)</title><rect x="191.0" y="133" width="17.4" height="31.0" fill="rgb(247,167,51)" rx="2" ry="2" />
<text  x="194.05" y="151.5" ></text>
</g>
<g >
<title>io (1,193 samples, 7.79%)</title><rect x="279.8" y="133" width="91.8" height="31.0" fill="rgb(251,119,19)" rx="2" ry="2" />
<text  x="282.76" y="151.5" >io</text>
</g>
<g >
<title>time (838 samples, 5.47%)</title><rect x="1008.7" y="101" width="64.6" height="31.0" fill="rgb(237,59,31)" rx="2" ry="2" />
<text  x="1011.72" y="119.5" >time</text>
</g>
<g >
<title>__builtin__ (293 samples, 1.91%)</title><rect x="733.6" y="101" width="22.5" height="31.0" fill="rgb(253,201,26)" rx="2" ry="2" />
<text  x="736.57" y="119.5" >_..</text>
</g>
<g >
<title>abc (570 samples, 3.72%)</title><rect x="327.7" y="101" width="43.9" height="31.0" fill="rgb(223,174,8)" rx="2" ry="2" />
<text  x="330.74" y="119.5" >abc</text>
</g>
<g >
<title>encodings.latin_1 (807 samples, 5.27%)</title><rect x="217.6" y="133" width="62.2" height="31.0" fill="rgb(215,202,42)" rx="2" ry="2" />
<text  x="220.61" y="151.5" >encodi..</text>
</g>
<g >
<title>stat (495 samples, 3.23%)</title><rect x="556.5" y="69" width="38.1" height="31.0" fill="rgb(246,74,3)" rx="2" ry="2" />
<text  x="559.53" y="87.5" >stat</text>
</g>
<g >
<title>zipimport (103 samples, 0.67%)</title><rect x="10.0" y="133" width="7.9" height="31.0" fill="rgb(239,28,8)" rx="2" ry="2" />
<text  x="13.00" y="151.5" ></text>
</g>
<g >
<title>encodings.aliases (501 samples, 3.27%)</title><rect x="152.5" y="101" width="38.5" height="31.0" fill="rgb(218,202,15)" rx="2" ry="2" />
<text  x="155.47" y="119.5" >enc..</text>
</g>
<g >
<title>_abc (84 samples, 0.55%)</title><rect x="365.2" y="69" width="6.4" height="31.0" fill="rgb(238,164,13)" rx="2" ry="2" />
<text  x="368.16" y="87.5" ></text>
</g>
<g >
<title>small (3,008 samples, 19.63%)</title><rect x="841.6" y="133" width="231.7" height="31.0" fill="rgb(205,201,10)" rx="2" ry="2" />
<text  x="844.61" y="151.5" >small</text>
</g>
<g >
<title>encodings (1,707 samples, 11.14%)</title><rect x="59.6" y="133" width="131.4" height="31.0" fill="rgb(243,60,17)" rx="2" ry="2" />
<text  x="62.59" y="151.5" >encodings</text>
</g>
<g >
<title>_signal (120 samples, 0.78%)</title><rect x="208.4" y="133" width="9.2" height="31.0" fill="rgb(205,137,20)" rx="2" ry="2" />
<text  x="211.37" y="151.5" ></text>
</g>
<g >
<title>site (5,705 samples, 37.23%)</title><rect x="402.3" y="133" width="439.3" height="31.0" fill="rgb(254,153,6)" rx="2" ry="2" />
<text  x="405.28" y="151.5" >site</text>
</g>
<g >
<title>sitecustomize (1,110 samples, 7.24%)</title><rect x="756.1" y="101" width="85.5" height="31.0" fill="rgb(207,140,4)" rx="2" ry="2" />
<text  x="759.13" y="119.5" >sitecustom..</text>
</g>
<g >
<title>genericpath (202 samples, 1.32%)</title><rect x="618.3" y="37" width="15.5" height="31.0" fill="rgb(252,109,50)" rx="2" ry="2" />
<text  x="621.29" y="55.5" ></text>
</g>
<g >
<title>os (3,136 samples, 20.47%)</title><rect x="492.1" y="101" width="241.5" height="31.0" fill="rgb(247,223,6)" rx="2" ry="2" />
<text  x="495.07" y="119.5" >os</text>
</g>
<g >
<title>_collections_abc (1,295 samples, 8.45%)</title><rect x="633.8" y="69" width="99.8" height="31.0" fill="rgb(222,23,45)" rx="2" ry="2" />
<text  x="636.85" y="87.5" >_collections..</text>
</g>
<g >
<title>_stat (83 samples, 0.54%)</title><rect x="588.3" y="37" width="6.3" height="31.0" fill="rgb(229,45,27)" rx="2" ry="2" />
<text  x="591.26" y="55.5" ></text>
</g>
<g >
<title>_frozen_importlib_external (541 samples, 3.53%)</title><rect x="17.9" y="133" width="41.7" height="31.0" fill="rgb(254,135,14)" rx="2" ry="2" />
<text  x="20.93" y="151.5" >_fr..</text>
</g>
<g >
<title>all (15,323 samples, 100%)</title><rect x="10.0" y="165" width="1180.0" height="31.0" fill="rgb(227,91,7)" rx="2" ry="2" />
<text  x="13.00" y="183.5" ></text>
</g>
<g >
<title>posixpath (509 samples, 3.32%)</title><rect x="594.6" y="69" width="39.2" height="31.0" fill="rgb(219,225,51)" rx="2" ry="2" />
<text  x="597.65" y="87.5" >pos..</text>
</g>
<g >
<title>_codecs (62 samples, 0.40%)</title><rect x="147.7" y="69" width="4.8" height="31.0" fill="rgb(233,138,42)" rx="2" ry="2" />
<text  x="150.69" y="87.5" ></text>
</g>
</g>
</svg>
